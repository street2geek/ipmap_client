import { h } from "hyperapp";

const Filter = (state, actions) => () => (
  <div class="filter">
    <div class="container">
      <h3>Select to filter</h3>
      <ul>
        <li />
        <li />
        <li />
      </ul>
    </div>
  </div>
);

export default Filter;
